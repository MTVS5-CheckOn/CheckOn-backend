package com.checkon.learning.application;

// 원본 파일 접근 방식·형식·크기 제한은 LR-009(OPEN)에서 아직 결정되지 않았다.
// fileReference는 최종 계약이 정해지기 전까지의 자리표시자다.
public record LearningRecordImportProfilingRequest(String fileReference) {
}

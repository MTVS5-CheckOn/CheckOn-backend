spring:
  config:
    import: "optional:file:.env[.properties]"

checkon:
  security:
    test-authentication:
      enabled: ${TEST_AUTH_ENABLED:true}
      account-id: ${TEST_ACCOUNT_ID:}
      teacher-profile-id: ${TEST_TEACHER_PROFILE_ID:}


spring:
  config:
    import: "optional:file:.env[.properties]"

checkon:
  dashboard:
    test-authentication:
      enabled: ${DASHBOARD_TEST_AUTH_ENABLED:true}
      teacher-profile-id: ${DASHBOARD_TEST_TEACHER_PROFILE_ID:0198f000-0000-7000-8000-000000000001}

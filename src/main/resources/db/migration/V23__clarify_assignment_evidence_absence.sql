COMMENT ON TABLE detection_assignment_week_summaries IS
    'Teacher-scoped authoritative assignment projection. Missing rows stay absent so AI can distinguish unavailable evidence from explicit zero submissions.';
